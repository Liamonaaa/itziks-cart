{
  "name": "shawarma-hezi-functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions,firestore:rules --project itziks-cart",
    "serve": "firebase emulators:start --only functions,firestore"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "twilio": "^5.4.3"
  }
}
